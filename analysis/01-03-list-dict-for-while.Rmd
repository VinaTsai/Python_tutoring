## 列表list()

### 基本定义

1.用[]；
1.可同时包容不同类型的数据

```python
list1=['小明', 18, 2.1]
print(list1)
```
### 提取元素

1.提取单个元素：偏移量（offset, 从0开始）
1.提取多个元素：

```{r}
include_graphics(here::here("figure/01-03-offset.png"))
```

```python
#单个元素
students = ['小明', '小红', '小刚']
print(students[0])
#输出：小明

#多个元素

```